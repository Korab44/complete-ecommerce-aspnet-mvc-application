@model int

@if (Model > 0)
{
    <li class="nav-item d-flex">
        <a class="nav-link text-dark  d-flex" asp-controller="Orders" asp-action="ShoppingCart">
            <i class="bi bi-cart4 p-1 d-flex"></i> @Model
        </a>
    </li>
}